name: Bug, question, or feedback
description: Report a reproducible problem or suggest an improvement using synthetic examples.
body:
  - type: markdown
    attributes:
      value: "This is public. Remove personal notes, credentials, and private paths. Read SECURITY.md before reporting security concerns."
  - type: dropdown
    id: kind
    attributes:
      label: What would you like to share?
      options:
        - Setup or bug report
        - Question
        - Improvement suggestion
    validations:
      required: true
  - type: textarea
    id: details
    attributes:
      label: Details
      description: What did you expect, what happened, and how can we reproduce it? Use synthetic examples.
    validations:
      required: true
  - type: input
    id: environment
    attributes:
      label: Environment
      description: OS, Python/Node versions, and release or commit, if relevant. No private paths.
