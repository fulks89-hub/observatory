# Legacy compatibility directory

`.observatory/` is the primary configuration directory. `.brain/` is retained
for older agents and automation. Policy YAML files are mirrored and covered by
tests; new integrations should use `.observatory/`.
