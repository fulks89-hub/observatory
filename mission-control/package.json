{
  "name": "observatory-mission-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check scripts/collect.mjs && node --check scripts/collect-explore.mjs && node --check scripts/sync-airadar.mjs && node --check scripts/evaluate-airadar.mjs && node --check scripts/ai-evaluation-contract.mjs && node --check server.mjs",
    "test": "node --test tests/*.test.mjs",
    "refresh": "node scripts/collect.mjs && node scripts/collect-explore.mjs",
    "sync:airadar": "node scripts/sync-airadar.mjs",
    "evaluate:airadar": "node scripts/evaluate-airadar.mjs",
    "dev": "npm run refresh && vite --host 127.0.0.1",
    "build": "npm run refresh && vite build",
    "start": "npm run refresh && vite build && node server.mjs",
    "start:share": "MC_READ_ONLY=1 npm run refresh && MC_READ_ONLY=1 vite build && MC_READ_ONLY=1 node server.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "6.0.5",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "vite": "8.2.1"
  }
}
